@namespace("it.bitrock.dvs.model.avro")
protocol SpeedListProtocol {

  record TopSpeed {
    string flightCode;
    double speed;
  }

  record TopSpeedList {
    array<TopSpeed> elements = [];
  }
}

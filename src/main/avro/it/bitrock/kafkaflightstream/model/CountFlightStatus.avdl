@namespace("it.bitrock.kafkaflightstream.model")
protocol TotalCountProtocol {

  record CountFlightStatus {
    string flightStatus;
    long eventCount;
  }

  record CountAirline {
    long eventCount;
  }
}
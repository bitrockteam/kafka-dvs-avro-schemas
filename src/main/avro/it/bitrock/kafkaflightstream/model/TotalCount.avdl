@namespace("it.bitrock.kafkaflightstream.model")
protocol TotalCountProtocol {

  record CountFlight {
    string windowStartTime;
    long eventCount;
  }

  record CountAirline {
    string windowStartTime;
    long eventCount;
  }

  record CodeAirlineList {
    array<string> elements = [];
  }

}